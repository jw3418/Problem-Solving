#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <map>

using namespace std;

int main()
{
	int A, B, C, D, E, F;
	cin >> A >> B >> C >> D >> E >> F;
	cout << 1-A << " " << 1-B << " " << 2-C << " " << 2-D << " " << 2-E << " " << 8-F;

	return 0;
}